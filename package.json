{
  "name": "nextjs-mfe",
  "version": "1.0.0",
  "private": true,
  "repository": "git@github.com/WarrenF/nextjs-mfe.git",
  "author": "Warren Fitzpatrick",
  "license": "GPL",
  "workspaces": {
    "packages": [
      "packages/mfes/*"
    ]
  },
  "scripts": {
    "bootstrap": "yarn && cd packages/app-shell && yarn",
    "clean:all": "yarn clean:mfes && yarn clean:shell",
    "build:mfes": "concurrently \"wsrun --parallel build\"",
    "clean:mfes": "concurrently \"wsrun --parallel clean\"",
    "dev:mfes": "concurrently \"wsrun --parallel dev\"",
    "start:mfes": "concurrently \"wsrun --parallel start\"",
    "build:shell": "cd packages/app-shell && yarn build",
    "clean:shell": "cd packages/app-shell && yarn clean",
    "dev:shell": "cd packages/app-shell && yarn dev",
    "start:shell": "cd packages/app-shell && yarn start"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "wsrun": "^5.2.4"
  }
}
